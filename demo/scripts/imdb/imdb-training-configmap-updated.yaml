apiVersion: v1
data:
  __init__.py: ""
  arrow_cache_client.py: |
    #!/usr/bin/env python3
    """
    Common Arrow Cache Client Library

    This module provides shared functionality for interacting with the distributed
    Arrow Cache system, including connection management, flight operations, and
    common utilities.
    """

    import logging
    import subprocess
    import time
    from typing import List, Optional

    import pyarrow as pa
    import pyarrow.flight as flight


    class BaseArrowCacheClient:
        """Base client for interacting with the distributed Arrow Cache system."""

        def __init__(self, host: str = "localhost", port: int = 50051):
            """Initialize the Arrow Cache client.

            Args:
                host: Host of the Arrow Cache head node
                port: Port of the Arrow Cache head node
            """
            self.host = host
            self.port = port
            self.client = None
            self.logger = logging.getLogger(self.__class__.__name__)

        def connect(self):
            """Connect to the Arrow Cache head node."""
            try:
                self.logger.info(f"Connecting to Arrow Cache at {self.host}:{self.port}")
                location = flight.Location.for_grpc_tcp(self.host, self.port)
                self.client = flight.FlightClient(location)
                self.logger.info("Successfully connected to Arrow Cache")
            except Exception as e:
                self.logger.error(f"Failed to connect to Arrow Cache: {e}")
                raise

        def get_flight_info_for_partition(self, partition_id: int, total_partitions: int):
            """Get flight info for a specific partition from the head node.

            Args:
                partition_id: The partition ID to query (0-based)
                total_partitions: Total number of partitions

            Returns:
                FlightInfo containing endpoints for the partition
            """
            try:
                self.logger.info(
                    f"Getting flight info for partition {partition_id} of {total_partitions}"
                )

                # Create flight descriptor with partition info
                descriptor = flight.FlightDescriptor.for_path(
                    str(partition_id), str(total_partitions)
                )

                # Get flight info from head node
                flight_info = self.client.get_flight_info(descriptor)

                self.logger.info(
                    f"Received flight info with {len(flight_info.endpoints)} endpoints"
                )
                return flight_info

            except Exception as e:
                self.logger.error(f"Failed to get flight info: {e}")
                raise

        def query_data_range(self, start_row: int, end_row: int) -> pa.Table:
            """Query data for a specific row range.

            Args:
                start_row: Starting row index (inclusive)
                end_row: Ending row index (inclusive)

            Returns:
                PyArrow table with the requested data
            """
            try:
                self.logger.info(f"Querying data range: {start_row} to {end_row}")

                # Create a ticket with the row range
                import struct

                ticket_data = struct.pack("QQ", start_row, end_row)  # Pack as two uint64
                ticket = flight.Ticket(ticket_data)

                # Get the data stream
                flight_stream = self.client.do_get(ticket)

                # Read all data into a table
                table = flight_stream.read_all()

                self.logger.info(
                    f"Retrieved {len(table)} rows with {len(table.column_names)} columns"
                )
                return table

            except Exception as e:
                self.logger.error(f"Failed to query data range: {e}")
                raise

        def translate_worker_uri(
            self, worker_uri: str, namespace: str = "arrow-cache"
        ) -> str:
            """Translate internal Kubernetes URIs to localhost port-forwarded URIs.

            Args:
                worker_uri: Original worker URI
                namespace: Kubernetes namespace

            Returns:
                Translated URI for local access
            """
            # Decode if bytes
            if isinstance(worker_uri, bytes):
                worker_uri = worker_uri.decode("utf-8")

            # Convert http:// to grpc:// for Arrow Flight compatibility
            if worker_uri.startswith("http://"):
                worker_uri = worker_uri.replace("http://", "grpc://", 1)

            # Translate internal Kubernetes URIs to localhost port-forwarded URIs
            if (
                f"arrow-cache-worker-0.arrow-cache-worker-svc.{namespace}.svc.cluster.local"
                in worker_uri
            ):
                return "grpc://localhost:50052"  # Port-forward worker-0
            elif (
                f"arrow-cache-worker-1.arrow-cache-worker-svc.{namespace}.svc.cluster.local"
                in worker_uri
            ):
                return "grpc://localhost:50053"  # Port-forward worker-1

            return worker_uri

        def query_workers_for_data(
            self,
            partition_infos: List,
            sample_queries: List,
            namespace: str = "arrow-cache",
        ):
            """Query data from workers for given sample queries.

            Args:
                partition_infos: List of (partition_id, flight_info) tuples
                sample_queries: List of (start, end, description) tuples
                namespace: Kubernetes namespace
            """
            for start, end, description in sample_queries:
                self.logger.info(f"Querying {description} (rows {start}-{end})")

                # Try each partition to find the one containing this range
                success = False
                for partition_id, flight_info in partition_infos:
                    if not flight_info.endpoints:
                        continue

                    # Try querying this partition's worker
                    for endpoint in flight_info.endpoints:
                        if not endpoint.locations:
                            continue

                        try:
                            worker_uri = self.translate_worker_uri(
                                endpoint.locations[0].uri, namespace
                            )

                            self.logger.info(
                                f"  Trying worker at {worker_uri} for partition {partition_id}"
                            )

                            # Connect to the worker directly
                            worker_location = flight.Location(worker_uri)
                            worker_client = flight.FlightClient(worker_location)

                            # Use the ticket from the endpoint (contains the partition range)
                            if endpoint.ticket:
                                flight_stream = worker_client.do_get(endpoint.ticket)
                                result = flight_stream.read_all()

                                self.logger.info(
                                    f"Successfully retrieved {len(result)} rows from "
                                    f"partition {partition_id}"
                                )

                                # Let subclasses handle the specific data processing
                                self.process_query_result(result, description)
                                success = True
                                break

                        except Exception as e:
                            self.logger.warning(f"Failed to query worker {worker_uri}: {e}")

                    if success:
                        break

                if not success:
                    self.logger.warning(f"Failed to query {description} from any worker")

                time.sleep(2)  # Delay between queries to see results clearly

        def process_query_result(self, result: pa.Table, description: str):
            """Process query result. Override in subclasses for specific behavior.

            Args:
                result: PyArrow table with query results
                description: Description of the query
            """
            if len(result) > 0:
                self.logger.info(f"Columns: {result.column_names}")
                self.logger.info(f"Schema: {result.schema}")

        def run_performance_test(
            self, num_queries: int = 10, query_ranges: Optional[List] = None
        ):
            """Run a performance test with configurable query ranges.

            Args:
                num_queries: Number of queries to execute
                query_ranges: List of (start, end, description) tuples, or None for default
            """
            self.logger.info(f"=== Running performance test with {num_queries} queries ===")

            import random

            query_times = []

            # Default query ranges if none provided
            if query_ranges is None:
                query_ranges = [
                    (0, 999, "First 1000 rows"),
                    (1000, 2999, "Rows 1000-2999"),
                    (5000, 7999, "Mid-range rows"),
                    (10000, 12999, "Later rows"),
                    (20000, 24999, "Final batch"),
                ]

            for i in range(num_queries):
                # Pick a random range from our predefined ranges
                start_range, end_range, description = random.choice(query_ranges)

                # Generate a smaller random subrange within the selected range
                query_start = random.randint(start_range, end_range - 200)
                query_end = query_start + random.randint(50, 200)

                start_time = time.time()
                try:
                    self.logger.info(
                        f"Query {i+1}/{num_queries}: {description}, "
                        f"rows {query_start}-{query_end}"
                    )

                    # Simulate query time (replace with actual query when implemented)
                    time.sleep(random.uniform(0.1, 0.5))  # Simulate network/processing time

                    query_time = time.time() - start_time
                    query_times.append(query_time)

                    self.logger.info(f"Query {i+1} completed in {query_time:.3f}s")

                except Exception as e:
                    self.logger.warning(f"Query {i+1} failed: {e}")

                time.sleep(0.5)  # Small delay between queries

            if query_times:
                avg_time = sum(query_times) / len(query_times)
                min_time = min(query_times)
                max_time = max(query_times)

                self.logger.info("=== Performance Test Results ===")
                self.logger.info(f"Successful queries: {len(query_times)}/{num_queries}")
                self.logger.info(f"Average query time: {avg_time:.3f}s")
                self.logger.info(f"Min query time: {min_time:.3f}s")
                self.logger.info(f"Max query time: {max_time:.3f}s")


    class S3Utils:
        """Utilities for working with S3 and Arrow Cache data files."""

        @staticmethod
        def list_s3_files(
            bucket_prefix: str, profile: str = "default"
        ) -> Optional[List[str]]:
            """List parquet files in an S3 path.

            Args:
                bucket_prefix: S3 path prefix (e.g., 's3://bucket/path/')
                profile: AWS profile name

            Returns:
                List of S3 file paths or None if failed
            """
            try:
                logger = logging.getLogger("S3Utils")
                logger.info(f"Listing files in: {bucket_prefix}")

                cmd = ["aws", "s3", "ls", bucket_prefix, "--recursive"]
                if profile and profile != "default":
                    cmd.extend([f"--profile={profile}"])

                result = subprocess.run(cmd, capture_output=True, text=True, check=True)

                files = []
                for line in result.stdout.strip().split("\\n"):
                    if line and ".parquet" in line:
                        # Extract filename from ls output
                        filename = line.split()[-1]
                        if filename.endswith(".parquet"):
                            # Reconstruct full S3 path
                            if bucket_prefix.endswith("/"):
                                full_path = f"{bucket_prefix.rstrip('/')}/{filename}"
                            else:
                                full_path = f"{bucket_prefix}/{filename}"
                            files.append(full_path)

                if files:
                    logger.info(f"Found {len(files)} parquet files:")
                    for f in files:
                        logger.info(f"  - {f}")
                    return files
                else:
                    logger.warning("No parquet files found")
                    return None

            except Exception as e:
                logger = logging.getLogger("S3Utils")
                logger.warning(f"Failed to list S3 files: {e}")
                return None

        @staticmethod
        def get_metadata_location_from_configmap(
            namespace: str, configmap_name: str = "arrow-cache-config"
        ) -> Optional[str]:
            """Get metadata location from Kubernetes ConfigMap.

            Args:
                namespace: Kubernetes namespace
                configmap_name: ConfigMap name

            Returns:
                Metadata location or None if failed
            """
            try:
                result = subprocess.run(
                    [
                        "kubectl",
                        "get",
                        "configmap",
                        configmap_name,
                        "-n",
                        namespace,
                        "-o",
                        "jsonpath={.data.METADATA_LOC}",
                    ],
                    capture_output=True,
                    text=True,
                    check=True,
                )

                metadata_loc = result.stdout.strip()
                return metadata_loc if metadata_loc else None
            except Exception as e:
                logger = logging.getLogger("S3Utils")
                logger.warning(f"Failed to get metadata location from configmap: {e}")
                return None
  imdb_training.py: "#!/usr/bin/env python3\n\"\"\"\nIMDB Dataset Training Script
    with Arrow Cache Integration\n\nThis script trains a language model on the IMDB
    movie review dataset using:\n1. Arrow Cache for distributed data access\n2. PyTorch/Transformers
    for model training\n3. Iceberg for experiment logging\n4. Real IMDB sentiment
    classification data\n\nSchema of IMDB dataset:\n- text: The movie review text\n-
    label: Sentiment label (0=negative, 1=positive)\n\"\"\"\n\nimport os\nimport sys\nimport
    io\nimport json\nimport time\nimport hashlib\nimport platform\nimport subprocess\nimport
    argparse\nimport logging\nfrom datetime import datetime, timezone\nfrom typing
    import Dict, List, Optional\n\n# Disable tokenizers parallelism warning\nos.environ[\"TOKENIZERS_PARALLELISM\"]
    = \"false\"\n\nimport boto3\nimport pandas as pd\nimport torch\nfrom torch import
    nn, optim\nfrom torch.utils.data import DataLoader, Dataset\nfrom transformers
    import (\n    AutoTokenizer, AutoModelForCausalLM, \n    TrainingArguments, Trainer\n)\n\n#
    Arrow and Iceberg imports\nimport pyarrow as pa\nimport pyarrow.flight as flight\nfrom
    pyiceberg.types import (\n    StringType, FloatType, LongType, TimestampType,\n
    \   ListType, MapType\n)\nfrom pyiceberg.partitioning import PartitionSpec\n#
    Import will be done locally where needed\n\n# Configure logging\nlogging.basicConfig(level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s')\nlogger = logging.getLogger(__name__)\n\n#
    ===================== Configuration =====================\nAWS_PROFILE = \"root-ricardo\"\nAWS_REGION
    = \"us-east-1\"\nGLUE_DB = \"hf_datasets\"\nRUNS_TABLE = \"imdb_training_runs\"\n\n#
    Iceberg warehouse for training logs\nWAREHOUSE_S3 = \"s3://ricardo.hf.datasets/iceberg-warehouse\"\n\n#
    Model checkpoints\nCKPT_BUCKET = \"ricardo.hf.datasets\"\nCKPT_PREFIX = \"imdb_models\"\n\n#
    Training configuration\nEPOCHS = 1\nBATCH_SIZE = 1  # Very small batch size for
    demo with limited memory\nLR = 5e-5\nSEED = 42\nMAX_LENGTH = 128  # Much shorter
    sequences for memory efficiency\nGRADIENT_ACCUMULATION_STEPS = 4\n\n# Arrow Cache
    configuration  \nARROW_CACHE_HOST = \"localhost\"\nARROW_CACHE_PORT = 50051\nARROW_CACHE_NAMESPACE
    = \"arrow-cache-imdb\"\n# =======================================================\n\n\nclass
    IMDBArrowCacheClient:\n    \"\"\"Arrow Cache client specialized for IMDB dataset.\"\"\"\n
    \   \n    def __init__(self, host: str = \"localhost\", port: int = 50051, in_cluster:
    bool = False):\n        # Import here to avoid issues if not available\n        sys.path.append('../lib')\n
    \       from arrow_cache_client import BaseArrowCacheClient\n        \n        self.base_client
    = BaseArrowCacheClient(host, port)\n        self.namespace = ARROW_CACHE_NAMESPACE\n
    \       self.in_cluster = in_cluster\n        self.logger = logging.getLogger(self.__class__.__name__)\n
    \       \n    def connect(self):\n        \"\"\"Connect to the Arrow Cache head
    node.\"\"\"\n        return self.base_client.connect()\n        \n    def get_flight_info_for_partition(self,
    partition_id: int, total_partitions: int):\n        \"\"\"Get flight info for
    a specific partition from the head node.\"\"\"\n        return self.base_client.get_flight_info_for_partition(partition_id,
    total_partitions)\n        \n    def translate_worker_uri(self, worker_uri: str,
    namespace: str = \"arrow-cache\") -> str:\n        \"\"\"Override to handle in-cluster
    vs local execution.\"\"\"\n        if self.in_cluster:\n            # Running
    in cluster - use URIs as-is, just convert http to grpc\n            if isinstance(worker_uri,
    bytes):\n                worker_uri = worker_uri.decode(\"utf-8\")\n            if
    worker_uri.startswith(\"http://\"):\n                worker_uri = worker_uri.replace(\"http://\",
    \"grpc://\", 1)\n            return worker_uri\n        else:\n            # Running
    locally - use parent's port-forwarding translation\n            return self.base_client.translate_worker_uri(worker_uri,
    namespace)\n        \n    def process_query_result(self, result: pa.Table, description:
    str):\n        \"\"\"Process IMDB-specific query results.\"\"\"\n        if len(result)
    > 0:\n            self.logger.info(f\"Retrieved {len(result)} IMDB samples\")\n
    \           self.logger.info(f\"Columns: {result.column_names}\")\n            \n
    \           # Show sample data\n            df = result.to_pandas()\n            if
    len(df) > 0:\n                sample = df.iloc[0]\n                self.logger.info(\"===
    Sample IMDB Entry ===\")\n                self.logger.info(f\"Text: {sample.get('text',
    'N/A')[:200]}...\")\n                self.logger.info(f\"Label: {sample.get('label',
    'N/A')} ({'Positive' if sample.get('label') == 1 else 'Negative'})\")\n        return
    result\n\n    def get_imdb_data(self, num_partitions: int = 4) -> pa.Table:\n
    \       \"\"\"Retrieve IMDB data from Arrow Cache.\"\"\"\n        self.logger.info(f\"Fetching
    IMDB data from Arrow Cache...\")\n        \n        all_data = []\n        \n
    \       for partition_id in range(num_partitions):\n            try:\n                self.logger.info(f\"Fetching
    partition {partition_id}/{num_partitions}\")\n                \n                flight_info
    = self.get_flight_info_for_partition(partition_id, num_partitions)\n                \n
    \               if flight_info.endpoints:\n                    for endpoint in
    flight_info.endpoints:\n                        if endpoint.locations and endpoint.ticket:\n
    \                           worker_uri = self.translate_worker_uri(\n                                endpoint.locations[0].uri,
    self.namespace\n                            )\n                            \n
    \                           self.logger.info(f\"Querying worker at {worker_uri}\")\n
    \                           \n                            try:\n                                worker_location
    = flight.Location(worker_uri)\n                                worker_client =
    flight.FlightClient(worker_location)\n                                \n                                flight_stream
    = worker_client.do_get(endpoint.ticket)\n                                partition_data
    = flight_stream.read_all()\n                                \n                                self.logger.info(f\"Retrieved
    {len(partition_data)} rows from partition {partition_id}\")\n                                all_data.append(partition_data)\n
    \                               break\n                                \n                            except
    Exception as e:\n                                self.logger.warning(f\"Failed
    to query worker {worker_uri}: {e}\")\n                else:\n                    self.logger.warning(f\"No
    endpoints found for partition {partition_id}\")\n                    \n            except
    Exception as e:\n                self.logger.warning(f\"Failed to get flight info
    for partition {partition_id}: {e}\")\n                \n            time.sleep(0.5)
    \ # Small delay between partitions\n        \n        if not all_data:\n            raise
    Exception(\"Failed to retrieve any data from Arrow Cache\")\n        \n        #
    Concatenate all partition data\n        result = pa.concat_tables(all_data)\n
    \       self.logger.info(f\"Total IMDB samples retrieved: {len(result)}\")\n        \n
    \       return result\n\n\nclass IMDBDataset(Dataset):\n    \"\"\"PyTorch dataset
    for IMDB movie reviews.\"\"\"\n    \n    def __init__(self, data: pa.Table, tokenizer,
    max_length: int = 512):\n        self.data = data\n        self.tokenizer = tokenizer\n
    \       self.max_length = max_length\n        \n        # Convert to pandas for
    easier manipulation\n        self.df = data.to_pandas()\n        \n        logger.info(f\"IMDB
    dataset initialized with {len(self.df)} samples\")\n        logger.info(f\"Columns:
    {list(self.df.columns)}\")\n        \n        # Verify required columns\n        if
    'text' not in self.df.columns:\n            raise ValueError(\"Required column
    'text' not found in IMDB data\")\n        if 'label' in self.df.columns:\n            logger.info(f\"Label
    distribution: {self.df['label'].value_counts().to_dict()}\")\n    \n    def __len__(self):\n
    \       return len(self.df)\n    \n    def __getitem__(self, idx):\n        row
    = self.df.iloc[idx]\n        text = str(row['text'])\n        \n        # For
    sentiment classification, we can format the text with label information\n        if
    'label' in row and not pd.isna(row['label']):\n            sentiment = \"positive\"
    if row['label'] == 1 else \"negative\"\n            formatted_text = f\"Review:
    {text}\\nSentiment: {sentiment}\"\n        else:\n            formatted_text =
    text\n        \n        # Tokenize\n        encoding = self.tokenizer(\n            formatted_text,\n
    \           truncation=True,\n            padding='max_length',\n            max_length=self.max_length,\n
    \           return_tensors='pt'\n        )\n        \n        return {\n            'input_ids':
    encoding['input_ids'].flatten(),\n            'attention_mask': encoding['attention_mask'].flatten(),\n
    \           'labels': encoding['input_ids'].flatten()\n        }\n\n\ndef ensure_bucket(session,
    bucket_name: str):\n    \"\"\"Ensure S3 bucket exists.\"\"\"\n    s3 = session.client('s3')\n
    \   try:\n        s3.head_bucket(Bucket=bucket_name)\n        logger.info(f\"S3
    bucket '{bucket_name}' exists.\")\n    except:\n        logger.info(f\"Creating
    S3 bucket '{bucket_name}'...\")\n        s3.create_bucket(Bucket=bucket_name)\n\n\ndef
    ensure_glue_db(session, db_name: str):\n    \"\"\"Ensure Glue database exists.\"\"\"\n
    \   glue = session.client('glue')\n    try:\n        glue.get_database(Name=db_name)\n
    \       logger.info(f\"Glue database '{db_name}' exists.\")\n    except:\n        logger.info(f\"Creating
    Glue database '{db_name}'...\")\n        glue.create_database(DatabaseInput={'Name':
    db_name})\n\n\ndef save_training_run_to_iceberg(session, run_data: Dict):\n    \"\"\"Save
    training run metadata to Iceberg table.\"\"\"\n    try:\n        from pyiceberg.catalog.glue
    import GlueCatalog\n        \n        catalog = GlueCatalog(\n            name=\"glue\",\n
    \           warehouse=WAREHOUSE_S3,\n            region_name=AWS_REGION\n        )\n
    \       \n        # Try to load existing table, create if it doesn't exist\n        try:\n
    \           table = catalog.load_table(f\"{GLUE_DB}.{RUNS_TABLE}\")\n        except:\n
    \           logger.info(f\"Creating Iceberg table {GLUE_DB}.{RUNS_TABLE}\")\n
    \           \n            schema = pa.schema([\n                (\"run_id\", pa.string()),\n
    \               (\"timestamp\", pa.timestamp('us', tz='UTC')),\n                (\"model_name\",
    pa.string()),\n                (\"dataset_samples\", pa.int64()),\n                (\"epochs\",
    pa.int64()),\n                (\"batch_size\", pa.int64()),\n                (\"learning_rate\",
    pa.float64()),\n                (\"max_length\", pa.int64()),\n                (\"training_time_seconds\",
    pa.float64()),\n                (\"data_source\", pa.string()),\n                (\"arrow_cache_used\",
    pa.string()),\n            ])\n            \n            table = catalog.create_table(\n
    \               f\"{GLUE_DB}.{RUNS_TABLE}\",\n                schema=schema,\n
    \               partition_spec=PartitionSpec()\n            )\n        \n        #
    Convert run data to PyArrow table\n        run_table = pa.table([\n            [run_data[\"run_id\"]],\n
    \           [run_data[\"timestamp\"]],\n            [run_data[\"model_name\"]],\n
    \           [run_data[\"dataset_samples\"]],\n            [run_data[\"epochs\"]],\n
    \           [run_data[\"batch_size\"]],\n            [run_data[\"learning_rate\"]],\n
    \           [run_data[\"max_length\"]],\n            [run_data[\"training_time_seconds\"]],\n
    \           [run_data[\"data_source\"]],\n            [run_data[\"arrow_cache_used\"]],\n
    \       ], schema=table.schema())\n        \n        table.append(run_table)\n
    \       logger.info(f\"Training run metadata saved to {GLUE_DB}.{RUNS_TABLE}\")\n
    \       \n    except Exception as e:\n        logger.warning(f\"Failed to save
    training run to Iceberg: {e}\")\n\n\ndef main():\n    parser = argparse.ArgumentParser(description=\"Train
    model on IMDB dataset using Arrow Cache\")\n    parser.add_argument(\"--model-name\",
    default=\"distilgpt2\", help=\"Model to use for training\")\n    parser.add_argument(\"--use-arrow-cache\",
    action=\"store_true\", help=\"Use Arrow Cache for data loading\")\n    parser.add_argument(\"--max-samples\",
    type=int, default=1000, help=\"Maximum samples to use for training\")\n    parser.add_argument(\"--dry-run\",
    action=\"store_true\", help=\"Run without actual training\")\n    parser.add_argument(\"--skip-checkpoint\",
    action=\"store_true\", help=\"Skip saving checkpoint to S3 (for testing)\")\n
    \   parser.add_argument(\"--use-irsa\", action=\"store_true\", help=\"Use IRSA
    (no AWS profile)\")\n    parser.add_argument(\"--aws-profile\", default=\"root-ricardo\",
    help=\"AWS profile name (ignored if --use-irsa)\")\n    parser.add_argument(\"--head-host\",
    default=\"localhost\", help=\"Arrow Cache head host\")\n    parser.add_argument(\"--head-port\",
    type=int, default=50051, help=\"Arrow Cache head port\")\n    parser.add_argument(\"--in-cluster\",
    action=\"store_true\", help=\"Running in-cluster (don't translate worker URIs)\")\n
    \   \n    args = parser.parse_args()\n    \n    # Set seeds for reproducibility\n
    \   torch.manual_seed(SEED)\n    \n    logger.info(\"\U0001F680 Starting IMDB
    training with Arrow Cache integration\")\n    logger.info(f\"Model: {args.model_name}\")\n
    \   logger.info(f\"Use Arrow Cache: {args.use_arrow_cache}\")\n    logger.info(f\"Max
    samples: {args.max_samples}\")\n    \n    # AWS session\n    if args.use_irsa:\n
    \       # Use IRSA - no profile needed, credentials from environment/metadata
    service\n        session = boto3.Session(region_name=AWS_REGION)\n        logger.info(\"Using
    IRSA for AWS authentication\")\n    else:\n        # Use specified AWS profile\n
    \       session = boto3.Session(profile_name=args.aws_profile, region_name=AWS_REGION)\n
    \       logger.info(f\"Using AWS profile: {args.aws_profile}\")\n    ensure_bucket(session,
    CKPT_BUCKET)\n    ensure_glue_db(session, GLUE_DB)\n    \n    # Initialize tokenizer
    and model\n    logger.info(\"Loading tokenizer and model...\")\n    tokenizer
    = AutoTokenizer.from_pretrained(args.model_name)\n    if tokenizer.pad_token is
    None:\n        tokenizer.pad_token = tokenizer.eos_token\n    \n    model = AutoModelForCausalLM.from_pretrained(args.model_name)\n
    \   logger.info(f\"Model loaded: {model.config.name_or_path}\")\n    \n    # Load
    data\n    if args.use_arrow_cache:\n        logger.info(\"\U0001F4CA Loading data
    from Arrow Cache...\")\n        try:\n            cache_client = IMDBArrowCacheClient(args.head_host,
    args.head_port, args.in_cluster)\n            cache_client.connect()\n            \n
    \           # Get data from cache\n            arrow_data = cache_client.get_imdb_data()\n
    \           \n            # Limit samples if specified\n            if args.max_samples
    and len(arrow_data) > args.max_samples:\n                # Take a random sample\n
    \               import random\n                indices = random.sample(range(len(arrow_data)),
    args.max_samples)\n                arrow_data = arrow_data.take(indices)\n                logger.info(f\"Using
    {len(arrow_data)} samples (limited from full dataset)\")\n                \n            dataset
    = IMDBDataset(arrow_data, tokenizer, MAX_LENGTH)\n            data_source = \"arrow_cache\"\n
    \           \n        except Exception as e:\n            logger.error(f\"Failed
    to load from Arrow Cache: {e}\")\n            logger.info(\"Falling back to direct
    Iceberg access...\")\n            \n            # Fallback to direct Iceberg access\n
    \           from pyiceberg.catalog.glue import GlueCatalog\n            catalog
    = GlueCatalog(\n                name=\"glue\",\n                warehouse=WAREHOUSE_S3,\n
    \               region_name=AWS_REGION\n            )\n            table = catalog.load_table(f\"{GLUE_DB}.imdb\")\n
    \           \n            scan = table.scan()\n            arrow_data = scan.to_arrow()\n
    \           \n            # Limit samples if specified\n            if args.max_samples
    and len(arrow_data) > args.max_samples:\n                import random\n                indices
    = random.sample(range(len(arrow_data)), args.max_samples)\n                arrow_data
    = arrow_data.take(indices)\n                logger.info(f\"Using {len(arrow_data)}
    samples (limited from full dataset)\")\n            \n            dataset = IMDBDataset(arrow_data,
    tokenizer, MAX_LENGTH)\n            data_source = \"iceberg_direct\"\n    else:\n
    \       logger.info(\"\U0001F4CA Loading data directly from Iceberg...\")\n        from
    pyiceberg.catalog.glue import GlueCatalog\n        catalog = GlueCatalog(\n            name=\"glue\",\n
    \           warehouse=WAREHOUSE_S3,\n            region_name=AWS_REGION\n        )\n
    \       table = catalog.load_table(f\"{GLUE_DB}.imdb\")\n        \n        scan
    = table.scan()\n        arrow_data = scan.to_arrow()\n        \n        # Limit
    samples if specified\n        if args.max_samples and len(arrow_data) > args.max_samples:\n
    \           import random\n            indices = random.sample(range(len(arrow_data)),
    args.max_samples)\n            arrow_data = arrow_data.take(indices)\n            logger.info(f\"Using
    {len(arrow_data)} samples (limited from full dataset)\")\n        \n        dataset
    = IMDBDataset(arrow_data, tokenizer, MAX_LENGTH)\n        data_source = \"iceberg_direct\"\n
    \   \n    logger.info(f\"Filtered dataset size: {len(dataset)} samples\")\n    \n
    \   # Create data loader\n    dataloader = DataLoader(dataset, batch_size=BATCH_SIZE,
    shuffle=True)\n    logger.info(f\"Dataset loaded: {len(dataset)} samples, {len(dataloader)}
    batches\")\n    \n    if args.dry_run:\n        logger.info(\"\U0001F9EA Dry run
    mode - skipping actual training\")\n        logger.info(f\"Would train on {len(dataset)}
    samples for {EPOCHS} epochs\")\n        return\n    \n    # Setup training\n    logger.info(\"\U0001F3CB️
    Setting up training...\")\n    \n    # Training arguments\n    training_args =
    TrainingArguments(\n        output_dir=\"./results\",\n        num_train_epochs=EPOCHS,\n
    \       per_device_train_batch_size=BATCH_SIZE,\n        gradient_accumulation_steps=GRADIENT_ACCUMULATION_STEPS,\n
    \       learning_rate=LR,\n        logging_steps=10,\n        save_strategy=\"no\",
    \ # Don't save intermediate checkpoints\n        logging_dir=\"./logs\",\n    )\n
    \   \n    # Create trainer\n    trainer = Trainer(\n        model=model,\n        args=training_args,\n
    \       train_dataset=dataset,\n        tokenizer=tokenizer,\n    )\n    \n    #
    Start training\n    logger.info(f\"\U0001F3AF Starting training for {EPOCHS} epochs...\")\n
    \   start_time = time.time()\n    \n    try:\n        trainer.train()\n        \n
    \       training_time = time.time() - start_time\n        logger.info(f\"✅ Training
    completed in {training_time:.2f} seconds\")\n        \n        # Save training
    run metadata\n        run_data = {\n            \"run_id\": hashlib.md5(f\"{args.model_name}_{int(start_time)}\".encode()).hexdigest()[:8],\n
    \           \"timestamp\": datetime.now(timezone.utc),\n            \"model_name\":
    args.model_name,\n            \"dataset_samples\": len(dataset),\n            \"epochs\":
    EPOCHS,\n            \"batch_size\": BATCH_SIZE,\n            \"learning_rate\":
    LR,\n            \"max_length\": MAX_LENGTH,\n            \"training_time_seconds\":
    training_time,\n            \"data_source\": data_source,\n            \"arrow_cache_used\":
    str(args.use_arrow_cache),\n        }\n        \n        save_training_run_to_iceberg(session,
    run_data)\n        \n        # Save final model if not skipping checkpoints\n
    \       if not args.skip_checkpoint:\n            model_save_path = f\"./final_model_{run_data['run_id']}\"\n
    \           trainer.save_model(model_save_path)\n            logger.info(f\"\U0001F4BE
    Model saved to {model_save_path}\")\n            \n            # Upload to S3\n
    \           try:\n                import subprocess\n                s3_path =
    f\"s3://{CKPT_BUCKET}/{CKPT_PREFIX}/{run_data['run_id']}/\"\n                subprocess.run([\n
    \                   \"aws\", \"s3\", \"cp\", model_save_path, s3_path, \"--recursive\"\n
    \               ], check=True)\n                logger.info(f\"\U0001F4E6 Model
    uploaded to {s3_path}\")\n            except Exception as e:\n                logger.warning(f\"Failed
    to upload model to S3: {e}\")\n        \n    except Exception as e:\n        logger.error(f\"❌
    Training failed: {e}\")\n        raise\n\n\nif __name__ == \"__main__\":\n    main()"
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: imdb-training-script
  namespace: arrow-cache-imdb
